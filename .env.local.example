# MongoDB
MONGODB_URI="mongodb+srv://USER:PASSWORD@cluster.mongodb.net/folsetech?retryWrites=true&w=majority"

# Email (recommended: SMTP provider like Postmark, SendGrid SMTP, Mailgun SMTP, Gmail SMTP*)
SMTP_HOST="smtp.yourprovider.com"
SMTP_PORT="587"
SMTP_USER="smtp-username"
SMTP_PASS="smtp-password"
MAIL_FROM="FolseTech AI Solutions <no-reply@folsetech.org>"
MAIL_TO="jon@folsetech.org"

# App
SITE_URL="https://folsetech.org"
